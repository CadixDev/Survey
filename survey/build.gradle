dependencies {
    // Mappings
    compile "org.cadixdev:lorenz:${rootProject.lorenzVersion}"
    compile "org.cadixdev:lorenz-asm:${rootProject.lorenzVersion}"

    // CSV
    compile 'me.jamiemansfield:csv:0.1.0'

    // JSON
    compile 'com.google.code.gson:gson:2.8.5'
}
